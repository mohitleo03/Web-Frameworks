var salary;
var name, getSalary;
getName();
string;
var customer = {
    salary: 1000,
    name: "mohit",
    getSalary: function (number) { return 1000; },
    getName: function () { return "hello"; }
};
console.log(customer.salary);
console.log(customer.name);
console.log(customer.getSalary());
console.log(customer.getName());
